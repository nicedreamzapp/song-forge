# user data — songs, exports, voice samples
outputs/
exports/
voice_swap_work/
.banned_phrases.json

# engines (each is its own upstream repo / huge model checkpoints)
engines/

# python / system noise
__pycache__/
*.pyc
.DS_Store
.venv/
venv/

# logs
*.log

# narration / voice tests / curated keepers (big WAVs, not for repo)
narration/
keepers/
voice_swap_test/

# editor backups
*.bak
*.bak.*

# personal scratch
lyrics_for_*.txt

# Android
android/build/
android/app/build/
android/.gradle/
android/local.properties
**/.DS_Store

# host infrastructure — NEVER commit these to a public repo.
# Supervisors, memory guards and agent notes describe the machines this runs
# on (ports, LAN addresses, filesystem paths) and are not part of the product.
/AGENT_NOTES*.md
/m5_supervisor.sh
/forge_guard.py
/mem_client.py
/clap_genre_check.py
/TODO.md
/README_TODO.md
